# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# --- Secrets / Signing (Google Play, Firebase, etc.) ---
# Android signing configs
/android/app/key.properties

# Local Android SDK path
/android/local.properties

# Keystores / certificates (never commit)
**/*.jks
**/*.keystore
**/*.p12
**/*.pem
**/*.der
**/*.crt
**/*.cer

# Google Play Developer API / service account credentials (json)
**/service-account*.json
**/serviceAccount*.json
**/google-play*.json
**/googleplay*.json
**/play-*.json

# Firebase Admin SDK service account credentials
**/*firebase-adminsdk-*.json

# Firebase config files (contain API keys; many teams choose to commit, but ignore if you treat them as secrets)
/android/app/google-services.json
/ios/Runner/GoogleService-Info.plist

# Common environment/secret files
.env
.env.*
!.env.example
!.env.sample

# Firebase local cache / emulators
.firebase/
firebase-debug.log
firestore-debug.log
